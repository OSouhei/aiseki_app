<% provide(:title, "users") %>

<div class="wrapper">
  <h1>All Users</h1>
  <div class="users-wrapper">
    <div class="grid">
      <%= render @users %>
    </div>
    <div class="paginate-wrapper">
      <%= paginate @users %>
    </div>
  </div>
</div>
