<% provide(:title, "users") %>


<div class="content">
  <h1>All Users</h1>
  <div class="rooms grid">
    <%= render @users %>
  </div>
  <div class="paginate-wrapper">
    <%= paginate @users %>
    <%= rel_next_prev_link_tags @users %>
  </div>
</div>
